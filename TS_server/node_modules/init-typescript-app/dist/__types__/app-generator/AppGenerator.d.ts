import AppConfig from '../app-configurator/AppConfig';
declare class AppGenerator {
    private readonly config;
    private readonly projectPath;
    private readonly templatesPath;
    constructor(appConfig: AppConfig);
    createApp(): Promise<void>;
    private createAppFolder;
    private removeAppFolder;
    private copyFile;
    private removeEjsExtensionFromPath;
}
export default AppGenerator;
